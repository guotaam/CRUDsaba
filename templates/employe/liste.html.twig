{% extends 'base.html.twig' %}



{% block body %}


<table class="table">
<thead>
<tr>
 <th>Prenom</th>
 <th>Nom</th>
 <th>Téléphone</th>
 <th>Email</th>
 <th>Adresse</th>
 <th>Poste</th>
 <th>Salaire</th>
 <th>Date de naissance</th>
 <th></th>
 <th></th>

</tr>

</thead>
<tbody>
{% for employe in employes %}
<tr>
<td>{{ employe.prenom}}</td>
<td>{{ employe.nom}}</td>
<td>{{ employe.telephone}}</td>
<td>{{ employe.email}}</td>
<td>{{ employe.adresse}}</td>
<td>{{ employe.poste}}</td>
<td>{{ employe.salaire}}</td>
<td> {{ employe.datedenaissance|date("m-d-Y")}}</td>
<td><a href="{{ path('modifier_employe',{ id:employe.id }) }}" class="btn btn-primary"> Modifier</a></td>
<td><a onclick="return confirm('voulez vous supprimer')" href="{{ path('supprimer_employe',{ id:employe.id }) }}" class="btn btn-primary">Supprimer  </a></td>
</tr>

</tr>
{% endfor %}


</tbody>



</table>

<button><a class ="btn btn-succes" href="{{ path('ajouter_employe')}} ">Ajouter un employe</a></button>

{% endblock  %}